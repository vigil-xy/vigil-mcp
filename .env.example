# Vigil MCP Bridge Configuration

# API Keys (comma-separated list)
# Generate secure keys with: openssl rand -hex 32
# Leave empty for development mode (no auth)
API_KEYS=your-secret-api-key-here,another-key-here

# Server Configuration
HOST=0.0.0.0
PORT=8080

# MCP Server Path (path to compiled vigil-mcp index.js)
MCP_SERVER_PATH=/app/build/index.js

# Timeout Configuration
MAX_SCAN_TIMEOUT=300

# Production Settings
# For production, ensure API_KEYS is set to a secure value
# and never commit this file to version control
